{
	"name": "backend",
	"version": "1.0.0",
	"description": "",
	"main": "server.js",
	"engines": {
    "node": "10.16.1",
    "npm": "6.9.0"
	},
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"build": "cd client && npm run build",
		"install-client": "cd client && npm install --only=dev",
		"heroku-postbuild": "npm run install-client && npm install && npm run build",
		"start": "node server.js",
		"dev": "concurrently -n 'server,client' -c 'yellow,green' \"nodemon server.js\" \"npm run client\"",
		"client": "cd client && npm start"
	},
	"author": "F3de",
	"license": "ISC",
	"dependencies": {
		"axios": "^0.19.2",
		"body-parser": "^1.19.0",
		"concurrently": "^5.1.0",
		"cors": "^2.8.5",
		"express": "^4.17.1",
		"mongoose": "^5.8.11",
		"morgan": "^1.9.1"
	}
}
